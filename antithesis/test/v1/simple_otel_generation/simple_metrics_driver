#!/usr/bin/env bash

# Simple Antithesis driver for basic metric generation
# Generates a single counter metric with default configuration

set -euo pipefail

# Generate a single counter metric with minimal configuration
./otel-datagen generate metrics --num-metrics=1 --metric-type=counter